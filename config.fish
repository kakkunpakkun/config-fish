# set PATH
set PATH $HOME/.cargo/bin $HOME/.tfenv/bin $HOME/.rbenv/bin $PATH

# use exa instead of ls
alias ls='exa'

# git status
alias g='git'
alias gst='git status'
alias ga='git add'
alias gd='git diff'
alias gc='git commit'
